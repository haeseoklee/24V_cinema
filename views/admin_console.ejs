<!DOCTYPE html>
<html>
    <head>
        <% include ./header.ejs %>
        <link rel="stylesheet" href="../css/sales.css">
    </head>
    <body>
        <% include ./nav.ejs %>
    <div id="admin-page">
        <% data.arr.forEach(function(item, index){ %>
            <div class="admin-cinema-name">
                <%= item.cinemaName %>
                
            </div>
            <div class="admin-cinema-sales">
            <% if (item.cinemaSelling) { %>
                <sapn>￦<%= item.cinemaSelling %></sapn>
                ￦￦<%= item.cinemaMenuSelling %>
            <% } else {%>
            
                ￦0
            </div>
            <% } %>
            <!-- <%= item.cinemaName %>
            지점별 예매 총매출: <%= item.cinemaSelling %>원
            지점별 매점 총매출: <%= item.cinemaMenuSelling %>원 -->
            <br>
        <% }) %>
        
        <div>
        총매출 ￦<%= data.totalSelling %>
        </div>
    </div> <!-- admin-page -->
    </body>
</html>